{
	"name": "gildedtros",
	"version": "1.0.0",
	"main": "index.js",
	"scripts": {
		"build": "tsc -p tsconfig.build.json && tsc-alias -p tsconfig.build.json",
		"start": "bun run test/text-test-fixture.ts",
		"test": "bun test test/",
		"test:watch": "bun test --watch test/",
		"lint": "biome check .",
		"lint:fix": "biome check --write .",
		"format": "biome format --write ."
	},
	"license": "MIT",
	"private": true,
	"devDependencies": {
		"@biomejs/biome": "^2.3.14",
		"@types/bun": "^1.3.9",
		"tsc-alias": "^1.8.16",
		"typescript": "^5.9.3"
	}
}
